{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[{"sourceType":"datasetVersion","sourceId":3507850,"datasetId":1972526,"databundleVersionId":3560437}],"dockerImageVersionId":31286,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"markdown","source":"### Complete Exploratory Data Analysis and Predictive Modeling on Restaurant Cuisine Ratings\n\n- **Dataset:** Cuisine_rating.csv  \n- **Domain:** Restaurant Analytics / Consumer Behavior  \n- **Type:** Supervised Machine Learning (Regression)\n---","metadata":{}},{"cell_type":"markdown","source":"## 1. Introduction\n\n### 1.1 What is Exploratory Data Analysis (EDA)?\n\nExploratory Data Analysis, commonly referred to as EDA, is the process of analyzing and investigating a dataset before applying any statistical models or machine learning algorithms. It is the foundation of any data science project. EDA helps us understand the structure, patterns, distributions, anomalies, and relationships hidden within data.\n\nThink of EDA as a detective's initial investigation at a crime scene. Before drawing conclusions, a detective first looks at all the available evidence, tries to understand what happened, identifies inconsistencies, and only then forms a theory. In data science, EDA is that investigative phase.\n\nEDA involves:\n- Summarizing the main characteristics of a dataset using descriptive statistics.\n- Visualizing data using plots such as histograms, bar charts, boxplots, and heatmaps.\n- Identifying missing values, duplicates, and inconsistencies.\n- Discovering correlations and relationships between variables.","metadata":{}},{"cell_type":"markdown","source":"### 1.2 Why is EDA Important in Real-World Data Science?\n\nIn the real world, data is almost never clean or ready to use directly. Data arrives from multiple sources, is collected by different people, and contains errors, missing values, and inconsistencies. If you skip EDA and directly apply a machine learning model, you risk:\n\n- Training a model on incorrect or biased data, leading to completely wrong predictions.\n- Misunderstanding the problem, resulting in solving the wrong question.\n- Missing critical patterns that could lead to valuable business insights.\n- Building a model that performs well on paper but fails entirely in production.\n\nEDA saves you from all of these risks. According to industry surveys, professional data scientists spend approximately 60-80% of their project time on data preparation and EDA, not on model building. This reflects how critical EDA is.","metadata":{}},{"cell_type":"markdown","source":"### 1.3 What is Predictive Modeling?\n\nPredictive modeling is the process of using statistical algorithms and machine learning techniques to forecast future outcomes based on historical data. After we complete EDA and understand our dataset, we use the cleaned and prepared data to train machine learning models that can predict outcomes for new, unseen data.\n\nFor example: Given information about a restaurant customer (their age, gender, preferred cuisine, alcohol habits), can we predict the rating they would give to a restaurant? This is the kind of question predictive modeling answers.\n\nPredictive modeling can be of two types:\n- **Regression:** Predicting a continuous numerical value (e.g., predicting a rating score like 3.5 or 4.2).\n- **Classification:** Predicting a category or class (e.g., predicting whether a customer will return or not).\n\nIn this project, since our target variable (Overall Rating) is a continuous numerical value, we will perform regression.","metadata":{}},{"cell_type":"markdown","source":"### 1.4 Objective of This Project\n\nThe objective of this project is to:\n1. Thoroughly explore the restaurant cuisine ratings dataset to understand customer demographics, preferences, and behavior.\n2. Clean and prepare the data for machine learning.\n3. Engineer meaningful features that improve model performance.\n4. Build and evaluate multiple regression models to predict the overall rating a customer would give.\n5. Extract actionable business insights that restaurant owners and managers can use to improve their services.","metadata":{}},{"cell_type":"markdown","source":"### 1.5 What Insights Do We Want to Extract?\n\nWe want to answer the following business questions:\n- Which cuisine type receives the highest average ratings?\n- Does the gender of a customer influence how they rate a restaurant?\n- Does the customer's marital status or profession affect their rating behavior?\n- Does alcohol consumption frequency correlate with higher or lower ratings?\n- Which demographic segment is the most generous with ratings?\n- What are the most important factors that drive restaurant ratings?","metadata":{}},{"cell_type":"markdown","source":"### 1.6 Business Value of This Dataset\n\nFor restaurant owners, managers, and investors, understanding customer rating behavior is invaluable. A one-star difference in a restaurant's average rating can translate to a significant change in revenue. Research from Harvard Business School found that a one-star increase in Yelp rating leads to a 5-9% increase in revenue. Therefore:\n\n- Knowing which demographic gives low ratings helps identify service gaps.\n- Understanding cuisine preferences by region helps in menu planning.\n- Knowing the relationship between budget and satisfaction helps in pricing strategy.\n- Predicting ratings before they are submitted allows for proactive service improvement.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 2. Importing Libraries\n\n### Why Do We Import Libraries?\n\nPython by itself is a general-purpose programming language. It does not natively support data manipulation, statistical computations, or machine learning. Libraries are pre-written collections of code that extend Python's capabilities. By importing these libraries, we avoid reinventing the wheel and gain access to thousands of optimized functions built by experts.\n\nWithout importing these libraries, you would need to write thousands of lines of code from scratch just to load a CSV file or draw a simple chart. Libraries save enormous amounts of time and reduce the chance of error.\n\n### Library Explanations:\n\n**pandas:** Short for \"Panel Data.\" It is the most important library for data manipulation in Python. It provides the DataFrame object, which is like an Excel spreadsheet within Python. It allows you to load, clean, filter, transform, and summarize data with very concise code.\n\n**numpy:** Short for \"Numerical Python.\" It provides support for large multi-dimensional arrays and matrices, along with a large collection of mathematical functions. NumPy is the backbone that pandas and scikit-learn are built upon.\n\n**Difference between numpy and pandas:** NumPy operates on arrays (think of a grid of numbers). Pandas is built on top of NumPy and adds labeled axes (rows and columns with names), making it much easier to work with real-world tabular data that has column headers and row indices. For purely numerical operations on arrays, NumPy is faster. For working with structured datasets with mixed data types, pandas is preferred.\n\n**matplotlib:** The foundational plotting library in Python. It gives you complete control over every aspect of a plot, from axes labels to tick marks. It is powerful but requires more code for complex visualizations.\n\n**seaborn:** Built on top of matplotlib, seaborn provides a higher-level interface for creating statistically informative and visually attractive plots with much less code. It integrates tightly with pandas DataFrames.\n\n**scikit-learn (sklearn):** The most widely used machine learning library in Python. It provides tools for preprocessing data, splitting datasets, building models, and evaluating them.\n\n**What happens if we do NOT import these libraries?** Python will raise a `NameError` or `ModuleNotFoundError` the moment you try to use any of these functions. Your code will not run at all.","metadata":{}},{"cell_type":"code","source":"# Importing All Required Libraries\n\n# Core data manipulation libraries\nimport pandas as pd          # For DataFrame operations and data manipulation\nimport numpy as np           # For numerical computations and array operations\n\n# Visualization libraries\nimport matplotlib.pyplot as plt   # Base plotting library\nimport seaborn as sns             # Statistical visualization on top of matplotlib\n\n# Machine learning - preprocessing\nfrom sklearn.preprocessing import LabelEncoder, StandardScaler\n# LabelEncoder: converts text categories to integers (e.g., Male=0, Female=1)\n# StandardScaler: scales numerical features to have mean=0 and std=1\n\n# Machine learning - model selection\nfrom sklearn.model_selection import train_test_split, cross_val_score, GridSearchCV\n# train_test_split: splits data into training and testing sets\n# cross_val_score: evaluates model using k-fold cross validation\n# GridSearchCV: finds the best hyperparameters by trying all combinations\n\n# Machine learning - regression models\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.tree import DecisionTreeRegressor\nfrom sklearn.ensemble import RandomForestRegressor\n\n# Machine learning - evaluation metrics\nfrom sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score\n\n# Pipeline for production-ready code\nfrom sklearn.pipeline import Pipeline\n\n# Suppress warnings for cleaner output\nimport warnings\nwarnings.filterwarnings('ignore')\n\n# Configure matplotlib to display plots inline in the notebook\n%matplotlib inline\n\n# Set a consistent visual style for all seaborn plots\n# 'whitegrid' adds horizontal grid lines which help read values off charts\nsns.set_style('whitegrid')\n\n# Set the default figure size for all plots\nplt.rcParams['figure.figsize'] = (10, 6)\n\n# Set a random seed for reproducibility\n# This ensures that whenever you run this notebook, you get the same random results\nRANDOM_STATE = 42\n\nprint(\"All libraries imported successfully.\")\nprint(f\"Pandas version: {pd.__version__}\")\nprint(f\"NumPy version: {np.__version__}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**What the output tells us:** Seeing the version numbers confirms that the libraries are installed correctly. Different versions of libraries can behave differently, so documenting versions is important for reproducibility in professional projects.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 3. Loading the Dataset\n\n### Why Do We Load Data This Way?\n\nWe use `pd.read_csv()` to load comma-separated value files directly into a pandas DataFrame. A DataFrame is a two-dimensional labeled data structure — think of it as an Excel table inside Python. It has rows and columns, each with labels.\n\n### What Happens if the Dataset Path is Wrong?\n\nPython will raise a `FileNotFoundError`. This is one of the most common beginner mistakes. Always ensure:\n1. The file name is spelled correctly (case-sensitive on Linux/Mac).\n2. The file is in the same directory as the notebook, OR you provide the full absolute path.\n3. The file extension is correct (.csv, not .CSV or .xlsx).\n\n### Common File Loading Errors:\n- `FileNotFoundError`: File does not exist at the specified path.\n- `UnicodeDecodeError`: The file uses a different character encoding (try `encoding='latin-1'` or `encoding='utf-8-sig'`).\n- `ParserError`: The file is malformed (e.g., inconsistent number of columns in some rows).","metadata":{}},{"cell_type":"code","source":"# Loading the Dataset\n\n# pd.read_csv() reads a CSV file and converts it into a pandas DataFrame.\n# We assign it to the variable 'df' which stands for DataFrame.\n# Using 'encoding=\"utf-8-sig\"' handles the BOM (Byte Order Mark)\n# character that sometimes appears at the start of CSV files created\n# on Windows systems. Without this, the first column name may appear\n# as '\\ufeffUser ID' instead of 'User ID'.\n\ndf = pd.read_csv('/kaggle/input/datasets/surajjha101/cuisine-rating/Cuisine_rating.csv', encoding='utf-8-sig')\n\n# Display the first 5 rows of the dataset\n# .head() shows us the first 5 rows by default\n# This gives us a quick visual confirmation that the data loaded correctly\nprint(\"Dataset loaded successfully.\")\nprint(f\"Shape of the dataset: {df.shape}\")\nprint(\"\\nFirst 5 rows of the dataset:\")\ndf.head()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**What does 'shape' mean?**\n\n`df.shape` returns a tuple `(rows, columns)`. For example, `(200, 15)` means the dataset has 200 rows (records/customers) and 15 columns (features/attributes). This is the most fundamental piece of information about a dataset's size.\n\n**What each column represents:**\n- `User ID`: A unique identifier assigned to each customer. Not useful for modeling - identifiers have no predictive power.\n- `Area code`: The area code of the customer's residence. A proxy for geographic location.\n- `Location`: The neighborhood and city where the customer lives.\n- `Gender`: The customer's gender (Male/Female).\n- `YOB`: Year of Birth. We will transform this into Age.\n- `Marital Status`: Customer's marital status (Single/Married/Divorced).\n- `Activity`: Career status - whether the customer is a Student or Professional.\n- `Budget`: The budget level the customer is willing to spend (1-5 scale).\n- `Cuisines`: The type of cuisine the customer prefers (Japanese, Indian, French, etc.).\n- `Alcohol`: How frequently the customer consumes alcohol (Never/Often/Socially).\n- `Smoker`: Whether the customer smokes (Never/Often/Socially).\n- `Food Rating`: Rating given specifically for food quality.\n- `Service Rating`: Rating given specifically for service quality.\n- `Overall Rating`: The combined overall rating. This is our **target variable** for prediction.\n- `Often A S`: Indicates frequency status - appears to be related to smoking/alcohol habits.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 4. Understanding the Dataset Structure\n\nBefore cleaning or modeling, we need to deeply understand what kind of data we have. This involves examining data types, identifying missing values, and understanding the distribution of values in each column.\n\n### Why Do Data Types Matter in Modeling?\n\nMachine learning algorithms are fundamentally mathematical operations. They work on numbers, not text. If a column that should be numeric (e.g., `Budget` with values 1-5) is stored as a string (object type), mathematical operations like computing the mean will fail or produce wrong results. Identifying and correcting data types is a critical step.\n\n### What Happens if a Numeric Column is Treated as Object?\n\nIf the `Budget` column is stored as an object type:\n- `df['Budget'].mean()` will raise a `TypeError`.\n- The column cannot be used directly in any mathematical model.\n- Statistical summaries will be incomplete.\n- Sorting will be lexicographic (1, 10, 2, 3) instead of numeric (1, 2, 3, 10).","metadata":{}},{"cell_type":"code","source":"# Dataset Information\n\n# df.info() prints a concise summary of the DataFrame including:\n# - Total number of rows (RangeIndex)\n# - Column names\n# - Count of non-null values per column (helps spot missing values)\n# - Data type of each column\n# - Memory usage of the entire DataFrame\n\nprint(\"=\" * 60)\nprint(\"DATASET INFORMATION\")\nprint(\"=\" * 60)\ndf.info()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting the output:**\n- Columns listed as `object` type contain text (strings). These include Gender, Location, Cuisines, etc.\n- Columns listed as `int64` or `float64` are numeric. These include YOB, Budget, Food Rating, etc.\n- If any column shows fewer non-null values than the total row count, it has missing values.\n- Memory usage tells us how much RAM the DataFrame is consuming. For large datasets, this becomes critical.","metadata":{}},{"cell_type":"code","source":"# Descriptive Statistics for Numerical Columns\n# df.describe() provides summary statistics for all numerical columns:\n# - count: number of non-null values\n# - mean: average value\n# - std: standard deviation (how spread out the data is)\n# - min: minimum value\n# - 25%: first quartile (25% of data is below this value)\n# - 50%: median (middle value)\n# - 75%: third quartile (75% of data is below this value)\n# - max: maximum value\n\nprint(\"=\" * 60)\nprint(\"DESCRIPTIVE STATISTICS - NUMERICAL COLUMNS\")\nprint(\"=\" * 60)\ndf.describe().round(2)","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting descriptive statistics:**\n- **Mean vs Median:** If the mean and median (50%) are very different, the data is skewed. For example, if the mean Overall Rating is 3.8 but the median is 4.5, a few very low ratings are pulling the mean down.\n- **Standard Deviation:** A high std relative to the mean indicates high variability. A low std indicates values are clustered tightly around the mean.\n- **Min/Max:** These immediately reveal if there are any unrealistic values. For a 1-5 rating scale, seeing a value of 0 or 10 would indicate a data error.","metadata":{}},{"cell_type":"code","source":"#  Descriptive Statistics for Categorical Columns\n# df.describe(include='object') provides summary statistics for text columns:\n# - count: number of non-null values\n# - unique: number of distinct values (cardinality)\n# - top: the most frequently occurring value (mode)\n# - freq: how many times the most frequent value appears\n\nprint(\"=\" * 60)\nprint(\"DESCRIPTIVE STATISTICS - CATEGORICAL COLUMNS\")\nprint(\"=\" * 60)\ndf.describe(include='object')","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**What this tells us:**\n- The `unique` row shows cardinality — how many different values exist. A Location with 15 unique values means 15 different neighborhoods. A column with very high cardinality (e.g., 190 unique values in 200 rows) is likely an identifier and not useful for modeling.\n- The `top` and `freq` rows show the dominant category. If the top gender is 'Male' with frequency 118 out of 200, we know the dataset is not gender-balanced.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 5. Data Cleaning\n\nData cleaning is the process of detecting and correcting (or removing) corrupt, inaccurate, or irrelevant records from a dataset. In the real world, data is almost always dirty. Cleaning it is not optional — it is essential. A model trained on dirty data will give dirty predictions, a principle known as **\"Garbage In, Garbage Out\" (GIGO)**.\n\n### 5.1 Checking Missing Values\n\n**What are null values?**  \nA null value (also called missing value, NaN = Not a Number) represents the absence of data at a specific location in the dataset. It is different from zero. Zero means \"the value is zero.\" Null means \"we do not know what this value is.\"\n\n**Why do missing values occur?**\n- Survey respondents skipped a question.\n- A sensor malfunction during data collection.\n- Data entry errors.\n- Merging datasets where not all records exist in both datasets.\n\n**How do missing values affect a model?**  \nMost machine learning algorithms cannot handle null values at all. If you pass a dataset with missing values to scikit-learn's models, they will raise a `ValueError` and refuse to run. Even if they could run, missing values would bias the model because the algorithm would have no information for those records.\n\n**What happens if we ignore null values?**  \nIn the best case, your model throws an error and you are forced to deal with it. In the worst case (in some libraries or configurations), null values are silently treated as 0, which introduces severe bias.","metadata":{}},{"cell_type":"code","source":"# Checking Missing Values\n# df.isnull() returns a DataFrame of True/False values\n# where True means the value is missing (null)\n# .sum() counts the number of True values per column\n# Dividing by len(df) * 100 gives the percentage of missing values\n\nmissing_count = df.isnull().sum()\nmissing_percentage = (df.isnull().sum() / len(df)) * 100\n\nmissing_df = pd.DataFrame({\n    'Missing Count': missing_count,\n    'Missing Percentage (%)': missing_percentage.round(2)\n})\n\nprint(\"=\" * 60)\nprint(\"MISSING VALUE ANALYSIS\")\nprint(\"=\" * 60)\nprint(missing_df[missing_df['Missing Count'] > 0])\n\nif missing_df['Missing Count'].sum() == 0:\n    print(\"\\nNo missing values detected in the dataset.\")\nelse:\n    print(f\"\\nTotal columns with missing values: {(missing_df['Missing Count'] > 0).sum()}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 5.2 Handling Missing Values\n\nEven if this dataset has few or no missing values, understanding how to handle them is critical for your career. Here is the complete framework:\n\n**Strategy 1 - Drop rows:** Only acceptable if the missing percentage is very small (less than 2%) and the dataset is large. Dropping rows when the dataset is small loses valuable information.\n\n**Strategy 2 - Imputation:** Filling in missing values with a calculated value.\n- **Mean imputation** (for numerical columns): Replace missing values with the column's average. Use when the data is normally distributed (symmetric bell curve). The danger is that if there are extreme outliers, the mean is pulled away from the center, making imputation inaccurate.\n- **Median imputation** (for numerical columns): Replace missing values with the column's median. Use when the data is skewed (not symmetric). The median is not affected by extreme outliers, making it more robust.\n- **Mode imputation** (for categorical columns): Replace missing values with the most frequently occurring value. This is the only reasonable option for text/category data.\n\n**What happens if we impute incorrectly?** We introduce bias. For instance, if 30% of 'Age' values are missing and we impute all with the mean, we are essentially inventing data for 30% of our dataset. This can make the model think age has much less variability than it actually does, leading to underestimation of age's predictive power.\n\n**Real-world impact of skipping imputation:** A production model receiving records with missing values will either crash or produce wildly wrong predictions, leading to business losses and user distrust.","metadata":{}},{"cell_type":"code","source":"#  Handling Missing Values\n# We apply strategic imputation based on the data type of each column\n\n# Separate numerical and categorical columns for different treatment\nnumerical_cols = df.select_dtypes(include=['int64', 'float64']).columns.tolist()\ncategorical_cols = df.select_dtypes(include=['object']).columns.tolist()\n\nprint(f\"Numerical columns: {numerical_cols}\")\nprint(f\"Categorical columns: {categorical_cols}\")\n\n# For numerical columns: use median imputation (robust to outliers)\nfor col in numerical_cols:\n    if df[col].isnull().sum() > 0:\n        median_val = df[col].median()\n        df[col].fillna(median_val, inplace=True)\n        print(f\"Filled '{col}' with median value: {median_val}\")\n\n# For categorical columns: use mode imputation (most frequent value)\nfor col in categorical_cols:\n    if df[col].isnull().sum() > 0:\n        mode_val = df[col].mode()[0]\n        df[col].fillna(mode_val, inplace=True)\n        print(f\"Filled '{col}' with mode value: {mode_val}\")\n\n# Verify no missing values remain\nprint(\"\\nMissing values after imputation:\")\nprint(df.isnull().sum().sum(), \"total missing values remaining\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 5.3 Checking and Removing Duplicates\n\n**Why are duplicates dangerous?**\n\nA duplicate row means the same record appears more than once. In machine learning, if the same customer appears 5 times in the training data and once in the test data, the model has effectively \"memorized\" that specific pattern. This is a form of data leakage that leads to overly optimistic performance estimates.\n\n**Effect on model bias:**\nDuplicates inflate the importance of duplicated records. If 10 specific customers all gave a rating of 5, and each appears 3 times in the training data, the model will overfit to the characteristics of those 10 customers and will not generalize well to new customers.\n\n**Real-world example:**\nIn a fraud detection system, if legitimate transactions are duplicated 10 times, the model learns that \"the data is overwhelmingly legitimate\" and will fail to detect rare fraud cases. This is a real problem that has cost financial institutions millions.","metadata":{}},{"cell_type":"code","source":"# Checking and Removing Duplicates\n# df.duplicated() returns a boolean Series where True means\n# that row is an exact duplicate of a previously seen row\n\nnum_duplicates = df.duplicated().sum()\nprint(f\"Number of duplicate rows: {num_duplicates}\")\n\nif num_duplicates > 0:\n    print(\"Duplicate rows found. Removing them...\")\n    df.drop_duplicates(inplace=True)\n    df.reset_index(drop=True, inplace=True)\n    print(f\"Dataset shape after removing duplicates: {df.shape}\")\nelse:\n    print(\"No duplicate rows found. Dataset is clean on this front.\")\n\nprint(f\"\\nFinal dataset shape: {df.shape}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 5.4 Fixing Inconsistent Categorical Data\n\n**Why does categorical consistency matter?**\n\nComputers perform exact string matching. The strings `'NY'`, `'ny'`, `'N.Y.'`, and `'New York'` are four completely different values to Python. If your Location column has some entries as `'St. George,ny'` and others as `'St. George,NY'`, these will be treated as two different locations, doubling the number of apparent categories and distorting any analysis grouped by location.\n\nThis is called **data inconsistency** and it is extremely common in real-world datasets collected from multiple sources or entered manually by different people.\n\n**What happens if we skip this step?** Group-by operations will produce incorrect results. For example, `df.groupby('Location')['Overall Rating'].mean()` would show St. George,NY and St. George,ny as two separate groups with incorrect averages, making your analysis wrong.","metadata":{}},{"cell_type":"code","source":"# Fixing Inconsistent Categorical Data\n\n# Step 1: Standardize text case and strip whitespace\n# We first strip leading/trailing spaces, then standardize case\n\n# Strip whitespace from all object (string) columns\nfor col in categorical_cols:\n    if col in df.columns:\n        df[col] = df[col].str.strip()\n\n# Check Location column for inconsistencies\nprint(\"Unique values in 'Location' before cleaning:\")\nprint(sorted(df['Location'].unique()))\n\n# Standardize the state abbreviation to uppercase\n# Some entries have ',ny' (lowercase) instead of ',NY' (uppercase)\ndf['Location'] = df['Location'].str.replace(',ny', ',NY', case=False, regex=False)\ndf['Location'] = df['Location'].str.replace(', NY', ',NY', regex=False)\ndf['Location'] = df['Location'].str.replace(', MY', ',NY', regex=False)  # Fix typo MY -> NY\n\nprint(\"\\nUnique values in 'Location' after cleaning:\")\nprint(sorted(df['Location'].unique()))\n\n# Standardize Alcohol column\nprint(\"\\nUnique values in 'Alcohol':\")\nif 'Alcohol ' in df.columns:  # Note: there's a trailing space in the column name\n    df.rename(columns={'Alcohol ': 'Alcohol'}, inplace=True)\nprint(df['Alcohol'].unique())\n\n# Standardize Smoker column\nprint(\"\\nUnique values in 'Smoker':\")\nprint(df['Smoker'].unique())\n\nprint(\"\\nData standardization complete.\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Key takeaway:** After cleaning, the Location column should have consistent formatting. The `MY` typo (likely meant to be `NY`) has been corrected. The trailing space in the `Alcohol ` column name has been fixed to `Alcohol`. These small fixes prevent major analytical errors downstream.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 6. Feature Engineering\n\nFeature engineering is the process of creating new, more informative features from existing raw data. It is often described as the most creative and impactful part of data science. A well-engineered feature can dramatically improve model performance, sometimes more than switching to a more powerful algorithm.\n\nThe intuition behind feature engineering: raw data often contains information in a form that is not directly useful for a model. We transform it into a form that is more meaningful and directly related to the outcome we want to predict.\n\n### 6.1 Creating Age from Year of Birth (YOB)\n\n**Why is Age better than YOB for prediction?**\n\nConsider two customers: one born in 1990 and one born in 1970. The YOB values themselves (1990 and 1970) have no direct relationship to rating behavior. But their ages (35 and 55 years old, assuming current year is 2025) are meaningful — older customers may have different taste preferences and rating habits than younger customers.\n\nAlso, from a model's perspective, the number 1990 is just a large integer. Without context, a model does not inherently understand that 1990 means \"born 35 years ago.\" Age, as a derived feature, is already relative to the current time and directly captures the concept we care about.\n\n**What happens if we keep YOB directly?**\n- The model treats YOB as just a raw number. It does not understand the concept of time.\n- The range of values (1955-2009) may cause scaling issues.\n- The feature loses temporal meaning as years pass (a model trained in 2023 using YOB would behave differently if deployed in 2030 without retraining).\n- Age, once derived, is conceptually more stable and directly interpretable.","metadata":{}},{"cell_type":"code","source":"# Feature Engineering - Creating Age from YOB\n# We derive 'Age' by subtracting the year of birth from the current year.\n# This transforms a raw year value into a meaningful, interpretable feature.\n\nCURRENT_YEAR = 2025  # Set this to the year the analysis is performed\n\ndf['Age'] = CURRENT_YEAR - df['YOB']\n\nprint(\"Age feature created successfully.\")\nprint(f\"\\nAge statistics:\")\nprint(df['Age'].describe().round(2))\n\n# Visualize the age distribution\nplt.figure(figsize=(10, 5))\nsns.histplot(df['Age'], bins=20, kde=True, color='steelblue', edgecolor='white')\nplt.title('Distribution of Customer Ages', fontsize=14, fontweight='bold')\nplt.xlabel('Age (Years)', fontsize=12)\nplt.ylabel('Frequency', fontsize=12)\nplt.axvline(df['Age'].mean(), color='red', linestyle='--', label=f'Mean Age: {df[\"Age\"].mean():.1f}')\nplt.axvline(df['Age'].median(), color='orange', linestyle='--', label=f'Median Age: {df[\"Age\"].median():.1f}')\nplt.legend(fontsize=11)\nplt.tight_layout()\nplt.show()\n\n# We can now drop the YOB column as it is redundant\ndf.drop('YOB', axis=1, inplace=True)\nprint(\"\\n'YOB' column dropped. 'Age' now replaces it.\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting the age distribution:**\n- The KDE (Kernel Density Estimate) curve shows the smooth shape of the distribution.\n- If the mean and median are close, the distribution is roughly symmetric.\n- If the tail extends to the right, the distribution is right-skewed (more older customers).\n- If the tail extends to the left, the distribution is left-skewed (more younger customers).","metadata":{}},{"cell_type":"markdown","source":"### 6.2 Encoding Categorical Variables\n\n**Why can machine learning models not handle string data directly?**\n\nMachine learning models are mathematical functions. They compute things like `weight1 * feature1 + weight2 * feature2`. You cannot multiply a weight by the string 'Male'. The string must be converted to a number first.\n\nThere are two main encoding strategies:\n\n**Label Encoding:** Assigns each unique category an integer. Example: Never=0, Often=1, Socially=2. This is appropriate for **ordinal** variables where there is a natural order (e.g., Never < Socially < Often makes sense for frequency). The problem with label encoding for nominal data (no natural order) is that it introduces a false numerical relationship. For example, if Male=0 and Female=1, the model might think Female is \"twice\" as male, which is meaningless.\n\n**One-Hot Encoding:** Creates a new binary (0 or 1) column for each unique category. Example: A 'Gender' column becomes 'Gender_Male' and 'Gender_Female', where each row has a 1 in exactly one of these columns. This removes the false ordinal relationship and is appropriate for **nominal** (unordered) variables.\n\n**The Dummy Variable Trap:** When you one-hot encode a column with k categories, you only need k-1 new columns. The last category is perfectly implied by all others being 0. Including all k columns introduces perfect multicollinearity, which breaks linear regression. This is why we set `drop_first=True`.\n\n**Curse of Dimensionality:** One-hot encoding increases the number of features (columns). If a column has 50 unique categories, it creates 49 new columns. With many such columns, the dataset becomes very wide. In high-dimensional spaces, data becomes sparse and models struggle to find patterns. This is known as the curse of dimensionality.\n\n**When to use Label Encoding:** For ordinal data (ordered categories) or for tree-based models (Decision Tree, Random Forest) which do not have the false ordering problem because they make decisions based on thresholds.","metadata":{}},{"cell_type":"code","source":"# Encoding Categorical Variables\n# We will encode categorical variables into numeric form\n# so that machine learning models can process them.\n\n# Step 1: Drop columns that are identifiers or have very high cardinality\n# 'User ID' is just an identifier and has no predictive power\n# 'Often A S' appears ambiguous - let us inspect it first\nprint(\"Unique values in 'Often A S':\", df['Often A S'].unique())\nprint(\"Value counts:\\n\", df['Often A S'].value_counts())\n\n# Drop identifier and ambiguous columns\ncols_to_drop = ['User ID', 'Area code', 'Often A S']\n# 'Area code' is redundant with 'Location'\ndf.drop(columns=[c for c in cols_to_drop if c in df.columns], inplace=True)\nprint(f\"\\nDropped columns: {cols_to_drop}\")\nprint(f\"Dataset shape: {df.shape}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Ordinal Encoding for Ordered Categories\n# Some categorical variables have a natural order.\n# We will manually map these to preserve their ordinal nature.\n\n# Alcohol frequency has a natural order: Never < Socially < Often\nalcohol_map = {'Never': 0, 'Socially': 1, 'Often': 2}\ndf['Alcohol_encoded'] = df['Alcohol'].map(alcohol_map)\n\n# Smoker frequency has the same natural order\nsmoker_map = {'Never': 0, 'Socially': 1, 'Often': 2}\ndf['Smoker_encoded'] = df['Smoker'].map(smoker_map)\n\n# Verify encoding\nprint(\"Alcohol encoding sample:\")\nprint(df[['Alcohol', 'Alcohol_encoded']].drop_duplicates().sort_values('Alcohol_encoded'))\n\n# Drop the original text columns now that we have encoded versions\ndf.drop(columns=['Alcohol', 'Smoker'], inplace=True)\nprint(\"\\nOriginal Alcohol and Smoker columns replaced with encoded versions.\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# One-Hot Encoding for Nominal Categories\n# For nominal categories (no natural order), we use one-hot encoding.\n# pd.get_dummies() automatically creates binary columns for each category.\n# drop_first=True avoids the dummy variable trap.\n\nnominal_cols = ['Gender', 'Location', 'Marital Status', 'Activity', 'Cuisines']\n\n# Keep a copy of the original dataframe for EDA (before encoding)\ndf_original = df.copy()\n\n# Apply one-hot encoding\ndf_encoded = pd.get_dummies(df, columns=nominal_cols, drop_first=True)\n\nprint(f\"Shape before encoding: {df.shape}\")\nprint(f\"Shape after one-hot encoding: {df_encoded.shape}\")\nprint(f\"\\nNew columns added by encoding:\")\nnew_cols = [c for c in df_encoded.columns if c not in df.columns]\nfor col in new_cols:\n    print(f\"  {col}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting the output:**\n\nThe shape change from the original to after encoding shows exactly how many new binary columns were created. For example, if 'Cuisines' had 8 unique values, 7 new binary columns are created (one is dropped to avoid the dummy variable trap). This is expected and correct.\n\nEach new binary column contains 1 if the customer preferred that cuisine, and 0 if they did not. This allows the model to treat each cuisine independently.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 7. Exploratory Data Analysis (EDA)\n\nNow that our data is clean and properly structured, we begin the core analytical phase. EDA is where we extract meaning from data through visualization and statistical analysis. Every chart we create should answer a specific question.\n\n### 7.1 Univariate Analysis\n\nUnivariate analysis examines one variable at a time. The primary goal is to understand the distribution of each feature independently — its shape, central tendency, spread, and the presence of outliers.\n\n**What does a distribution tell us?**\nA distribution shows how frequently each value (or range of values) appears in the data. Understanding distributions helps us:\n- Know if the data is balanced or imbalanced.\n- Identify outliers that need attention.\n- Choose appropriate statistical tests (parametric tests require normally distributed data).\n- Select appropriate imputation strategies.\n\n**What is skewness?**\nSkewness measures the asymmetry of a distribution. A skewness of 0 means a perfectly symmetric distribution. A positive skewness means the right tail is longer (data is concentrated on the left, with a few very high values). A negative skewness means the left tail is longer.\n\n**What happens if data is heavily skewed?**\nHeavy skewness violates the assumptions of linear regression, which assumes normally distributed errors. It can also cause the model to be dominated by extreme values. Common solutions include applying log transformation or square root transformation to reduce skewness.","metadata":{}},{"cell_type":"code","source":"# Univariate Analysis - Distribution of Ratings\n# We examine the distribution of all three rating columns:\n# Food Rating, Service Rating, and Overall Rating\n\nfig, axes = plt.subplots(1, 3, figsize=(18, 6))\n\nrating_cols = ['Food Rating', 'Service Rating', 'Overall Rating']\ncolors = ['#2196F3', '#4CAF50', '#FF5722']\n\nfor i, (col, color) in enumerate(zip(rating_cols, colors)):\n    axes[i].hist(df_original[col].dropna(), bins=10, color=color,\n                 edgecolor='white', alpha=0.85)\n    axes[i].set_title(f'Distribution of {col}', fontsize=13, fontweight='bold')\n    axes[i].set_xlabel(col, fontsize=11)\n    axes[i].set_ylabel('Frequency', fontsize=11)\n    axes[i].axvline(df_original[col].mean(), color='red',\n                    linestyle='--', linewidth=2,\n                    label=f'Mean: {df_original[col].mean():.2f}')\n    axes[i].legend(fontsize=10)\n\n    # Print skewness\n    skewness = df_original[col].skew()\n    print(f\"{col}: Mean={df_original[col].mean():.2f}, \"\n          f\"Median={df_original[col].median():.2f}, \"\n          f\"Skewness={skewness:.3f}\")\n\nplt.suptitle('Distribution of All Rating Columns', fontsize=15, fontweight='bold', y=1.02)\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Univariate Analysis - Categorical Variables\n# We examine the count distribution of categorical variables.\n# This tells us if categories are balanced or if some dominate.\n\nfig, axes = plt.subplots(2, 3, figsize=(18, 12))\naxes = axes.flatten()\n\ncat_cols_to_plot = ['Gender', 'Marital Status', 'Activity', 'Cuisines', 'Alcohol_encoded', 'Smoker_encoded']\n\nfor i, col in enumerate(cat_cols_to_plot):\n    if col in df_original.columns:\n        value_counts = df_original[col].value_counts()\n        axes[i].bar(value_counts.index.astype(str), value_counts.values,\n                    color=sns.color_palette('Set2', len(value_counts)),\n                    edgecolor='white')\n        axes[i].set_title(f'Count Distribution: {col}', fontsize=12, fontweight='bold')\n        axes[i].set_xlabel(col, fontsize=10)\n        axes[i].set_ylabel('Count', fontsize=10)\n        axes[i].tick_params(axis='x', rotation=30)\n\n        # Print percentage breakdown\n        pct = (value_counts / len(df_original) * 100).round(1)\n        print(f\"\\n{col} distribution:\")\n        for val, count, p in zip(value_counts.index, value_counts.values, pct.values):\n            print(f\"  {val}: {count} ({p}%)\")\n\nplt.suptitle('Count Distribution of Categorical Variables', fontsize=15, fontweight='bold', y=1.01)\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting these plots:**\n\nThese bar charts reveal the composition of our dataset:\n- **Gender:** If significantly skewed toward one gender, our model's insights about gender-based preferences may not generalize well to the other gender.\n- **Cuisines:** Some cuisines may have very few data points. A model predicting ratings for a cuisine represented by only 5 customers will be unreliable for that cuisine.\n- **Alcohol frequency:** The proportion of non-drinkers vs. social drinkers vs. frequent drinkers tells us about the customer base.\n\n**Business implication:** If 80% of surveyed customers are students, any business recommendation derived from this data will be biased toward student preferences and may not apply to professional demographics.","metadata":{}},{"cell_type":"markdown","source":"### 7.2 Bivariate Analysis\n\nBivariate analysis examines the relationship between two variables simultaneously. Instead of asking \"what is the distribution of ratings?\" we now ask \"how does the rating differ between genders?\" or \"which cuisine gets the highest ratings?\"\n\n**Correlation vs. Causation:**\nThis is one of the most important distinctions in data science. Correlation means two variables tend to move together. Causation means one variable directly causes a change in the other. The famous example: ice cream sales and drowning rates are correlated (both increase in summer). But ice cream does not cause drowning — heat causes both. Always be careful about inferring causation from correlation in EDA. Bivariate analysis shows correlation; establishing causation requires controlled experiments.\n\n**Groupby operations:**\n`df.groupby('Gender')['Overall Rating'].mean()` splits the data into groups by Gender, then computes the mean rating within each group. This is the fundamental operation for bivariate analysis with one categorical and one numerical variable.","metadata":{}},{"cell_type":"code","source":"# Bivariate Analysis - Gender vs. Overall Rating\n\n# We want to know: Do male and female customers rate differently?\n# This has practical implications for targeted marketing.\n\nfig, axes = plt.subplots(1, 2, figsize=(16, 6))\n\n# Plot 1: Box plot (shows median, IQR, and outliers)\nsns.boxplot(data=df_original, x='Gender', y='Overall Rating',\n            palette='Set2', ax=axes[0])\naxes[0].set_title('Overall Rating by Gender (Box Plot)', fontsize=13, fontweight='bold')\naxes[0].set_xlabel('Gender', fontsize=11)\naxes[0].set_ylabel('Overall Rating', fontsize=11)\n\n# Plot 2: Bar plot showing mean rating by gender with error bars\ngender_rating = df_original.groupby('Gender')['Overall Rating'].agg(['mean', 'std']).reset_index()\naxes[1].bar(gender_rating['Gender'], gender_rating['mean'],\n            yerr=gender_rating['std'], capsize=5,\n            color=['#FF8A80', '#82B1FF'], edgecolor='white')\naxes[1].set_title('Mean Overall Rating by Gender', fontsize=13, fontweight='bold')\naxes[1].set_xlabel('Gender', fontsize=11)\naxes[1].set_ylabel('Mean Overall Rating', fontsize=11)\naxes[1].set_ylim(0, 6)\n\n# Print numerical summary\nprint(\"Mean Overall Rating by Gender:\")\nprint(df_original.groupby('Gender')['Overall Rating'].agg(['count', 'mean', 'median', 'std']).round(3))\n\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Bivariate Analysis - Cuisine vs. Overall Rating\n# Which cuisine type consistently receives the highest ratings?\n# This directly informs menu and cuisine selection decisions.\n\ncuisine_rating = df_original.groupby('Cuisines')['Overall Rating'].agg(\n    ['mean', 'median', 'count', 'std']\n).reset_index().sort_values('mean', ascending=False)\n\nprint(\"Cuisine Performance Summary:\")\nprint(cuisine_rating.round(3).to_string(index=False))\n\nfig, axes = plt.subplots(1, 2, figsize=(18, 7))\n\n# Plot 1: Horizontal bar chart of mean rating by cuisine\ncolors = sns.color_palette('RdYlGn', len(cuisine_rating))\naxes[0].barh(cuisine_rating['Cuisines'], cuisine_rating['mean'],\n             color=colors, edgecolor='white')\naxes[0].axvline(df_original['Overall Rating'].mean(), color='red',\n                linestyle='--', linewidth=2, label='Global Mean')\naxes[0].set_title('Mean Overall Rating by Cuisine Type', fontsize=13, fontweight='bold')\naxes[0].set_xlabel('Mean Overall Rating', fontsize=11)\naxes[0].legend(fontsize=10)\n\n# Plot 2: Box plot showing full distribution per cuisine\nsns.boxplot(data=df_original, x='Overall Rating', y='Cuisines',\n            palette='Set3', orient='h', ax=axes[1])\naxes[1].set_title('Rating Distribution by Cuisine (Box Plot)', fontsize=13, fontweight='bold')\naxes[1].set_xlabel('Overall Rating', fontsize=11)\naxes[1].set_ylabel('Cuisine Type', fontsize=11)\n\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Bivariate Analysis - Multiple Factors vs. Rating\n\nfig, axes = plt.subplots(2, 2, figsize=(18, 14))\n\n# Plot 1: Marital Status vs Rating\nsns.boxplot(data=df_original, x='Marital Status', y='Overall Rating',\n            palette='Pastel1', ax=axes[0, 0])\naxes[0, 0].set_title('Overall Rating by Marital Status', fontsize=12, fontweight='bold')\n\n# Plot 2: Activity vs Rating\nactivity_data = df_original.groupby('Activity')['Overall Rating'].mean().reset_index()\nsns.barplot(data=df_original, x='Activity', y='Overall Rating',\n            palette='Pastel2', ax=axes[0, 1], estimator='mean', ci=95)\naxes[0, 1].set_title('Mean Rating by Customer Activity (Student vs Professional)',\n                     fontsize=12, fontweight='bold')\n\n# Plot 3: Alcohol vs Rating\nalcohol_order = ['Never', 'Socially', 'Often']\n# Reconstruct alcohol labels from encoded values\nalcohol_reverse = {0: 'Never', 1: 'Socially', 2: 'Often'}\ndf_original['Alcohol_label'] = df_original['Alcohol_encoded'].map(alcohol_reverse)\nsns.boxplot(data=df_original, x='Alcohol_label', y='Overall Rating',\n            order=alcohol_order, palette='Blues', ax=axes[1, 0])\naxes[1, 0].set_title('Overall Rating by Alcohol Consumption Frequency',\n                     fontsize=12, fontweight='bold')\n\n# Plot 4: Budget vs Rating\nbudget_rating = df_original.groupby('Budget')['Overall Rating'].mean().reset_index()\naxes[1, 1].plot(budget_rating['Budget'], budget_rating['Overall Rating'],\n                marker='o', color='purple', linewidth=2.5, markersize=8)\naxes[1, 1].fill_between(budget_rating['Budget'], budget_rating['Overall Rating'],\n                         alpha=0.2, color='purple')\naxes[1, 1].set_title('Mean Overall Rating by Budget Level', fontsize=12, fontweight='bold')\naxes[1, 1].set_xlabel('Budget Level (1=Low, 5=High)', fontsize=11)\naxes[1, 1].set_ylabel('Mean Overall Rating', fontsize=11)\n\n# Print business insights\nprint(\"\\n--- BIVARIATE ANALYSIS INSIGHTS ---\")\nprint(\"\\nRating by Marital Status:\")\nprint(df_original.groupby('Marital Status')['Overall Rating'].mean().round(3))\nprint(\"\\nRating by Activity:\")\nprint(df_original.groupby('Activity')['Overall Rating'].mean().round(3))\nprint(\"\\nRating by Alcohol Consumption:\")\nprint(df_original.groupby('Alcohol_label')['Overall Rating'].mean().round(3))\nprint(\"\\nRating by Budget Level:\")\nprint(df_original.groupby('Budget')['Overall Rating'].mean().round(3))\n\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 7.3 Multivariate Analysis\n\nMultivariate analysis examines relationships among three or more variables simultaneously. The most common tool for this is the correlation heatmap.\n\n**What is a Correlation Coefficient?**\n\nThe Pearson correlation coefficient measures the linear relationship between two numerical variables. It ranges from -1 to +1:\n- **+1:** Perfect positive correlation (as one increases, the other increases proportionally).\n- **0:** No linear correlation.\n- **-1:** Perfect negative correlation (as one increases, the other decreases proportionally).\n\nIn practice, correlations above 0.7 or below -0.7 are considered strong. Between 0.3 and 0.7 (or -0.3 and -0.7) are moderate. Below 0.3 are weak.\n\n**What is Multicollinearity?**\n\nMulticollinearity occurs when two or more independent features are highly correlated with each other. For example, if `Food Rating` and `Service Rating` are both used to predict `Overall Rating`, and they are highly correlated with each other, the model struggles to determine the individual contribution of each. This causes instability in coefficient estimates in linear regression.\n\n**What happens if features are highly correlated?**\n- In Linear Regression: Coefficients become unstable and difficult to interpret. Small changes in training data can cause large changes in coefficients.\n- In Tree Models: Less of a problem because trees split on individual features independently.\n- Solution: Remove one of the highly correlated features, use dimensionality reduction (PCA), or use regularization techniques.","metadata":{}},{"cell_type":"code","source":"# Multivariate Analysis - Correlation Heatmap\n# A heatmap visualizes the correlation matrix.\n# Dark colors indicate strong correlation (positive or negative).\n# Light colors indicate weak correlation.\n\n# Compute correlation matrix on numerical columns only\nnumerical_data = df_original.select_dtypes(include=['int64', 'float64'])\ncorr_matrix = numerical_data.corr()\n\nplt.figure(figsize=(12, 9))\nmask = np.triu(np.ones_like(corr_matrix, dtype=bool))  # Only show lower triangle\nsns.heatmap(\n    corr_matrix,\n    annot=True,          # Show correlation values in cells\n    fmt='.2f',           # Format to 2 decimal places\n    mask=mask,           # Hide upper triangle to avoid redundancy\n    cmap='coolwarm',     # Red for positive, blue for negative\n    center=0,            # Center the colormap at 0\n    linewidths=0.5,\n    annot_kws={'size': 10}\n)\nplt.title('Correlation Matrix Heatmap of Numerical Features',\n          fontsize=14, fontweight='bold', pad=20)\nplt.tight_layout()\nplt.show()\n\n# Print high correlations\nprint(\"\\nCorrelations with 'Overall Rating':\")\nif 'Overall Rating' in corr_matrix.columns:\n    correlations = corr_matrix['Overall Rating'].drop('Overall Rating').sort_values(ascending=False)\n    print(correlations.round(3))","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 7.4 Outlier Detection\n\n**What are outliers?**\n\nOutliers are data points that are significantly different from the majority of observations. They lie far from the mean, often more than 3 standard deviations away. Outliers can be:\n- **Genuine outliers:** Real, unusual observations (a customer who rated every restaurant 1 out of spite).\n- **Error outliers:** Data entry or measurement errors (a budget value of 999 on a 1-5 scale).\n\n**Impact of outliers on the mean:**\nThe mean is extremely sensitive to outliers. A single rating of 100 (clearly an error) in a 1-5 dataset would dramatically inflate the mean. The median is not affected because it is determined by position, not value.\n\n**Impact on regression models:**\nLinear regression minimizes the sum of squared errors. Because errors are squared, a single extreme outlier contributes enormously to the loss function, causing the regression line to bend toward it. This results in a model that is fitted to the outlier rather than the general trend.\n\n**When to remove outliers and when not to:**\n- Remove if they are data entry errors (impossible values).\n- Remove if they are extreme and you are using a sensitive model (linear regression).\n- Do NOT remove if they represent genuine, important phenomena (in fraud detection, fraudulent transactions ARE the outliers you want to study).\n- Consider robust models (Random Forest, Huber regression) instead of removing.\n\n**The IQR Method:**\nThe Interquartile Range (IQR) is Q3 - Q1. A value is considered an outlier if it is:\n- Below Q1 - 1.5 * IQR (lower fence)\n- Above Q3 + 1.5 * IQR (upper fence)","metadata":{}},{"cell_type":"code","source":"# Outlier Detection using Box Plots and IQR Method\n\nnumerical_cols_for_outliers = ['Age', 'Budget', 'Food Rating', 'Service Rating', 'Overall Rating']\navailable_cols = [c for c in numerical_cols_for_outliers if c in df_original.columns]\n\nfig, axes = plt.subplots(1, len(available_cols), figsize=(18, 6))\n\nfor i, col in enumerate(available_cols):\n    sns.boxplot(y=df_original[col], ax=axes[i], color='lightcoral',\n                flierprops=dict(markerfacecolor='red', markersize=8, marker='o'))\n    axes[i].set_title(f'{col}', fontsize=11, fontweight='bold')\n    axes[i].set_ylabel(col, fontsize=10)\n\nplt.suptitle('Box Plots for Outlier Detection', fontsize=14, fontweight='bold', y=1.02)\nplt.tight_layout()\nplt.show()\n\n# IQR-based outlier counting\nprint(\"\\n--- IQR OUTLIER ANALYSIS ---\")\nfor col in available_cols:\n    Q1 = df_original[col].quantile(0.25)\n    Q3 = df_original[col].quantile(0.75)\n    IQR = Q3 - Q1\n    lower_fence = Q1 - 1.5 * IQR\n    upper_fence = Q3 + 1.5 * IQR\n    outliers = df_original[(df_original[col] < lower_fence) | (df_original[col] > upper_fence)]\n    print(f\"{col}: Q1={Q1:.2f}, Q3={Q3:.2f}, IQR={IQR:.2f}, \"\n          f\"Fences=[{lower_fence:.2f}, {upper_fence:.2f}], \"\n          f\"Outliers={len(outliers)} ({len(outliers)/len(df_original)*100:.1f}%)\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting box plots:**\n- The **box** spans from Q1 (25th percentile) to Q3 (75th percentile). This is the IQR.\n- The **line inside the box** is the median (50th percentile).\n- The **whiskers** extend to the fences (Q1 - 1.5*IQR and Q3 + 1.5*IQR).\n- **Dots beyond the whiskers** are outliers.\n\nFor rating data (1-5 scale), outliers should be carefully considered. A rating of 1 is not an error — it is a valid, if extreme, negative review.\n\n---","metadata":{}},{"cell_type":"markdown","source":"## 8. Preparing Data for Modeling","metadata":{}},{"cell_type":"markdown","source":"### 8.1 Defining the Target Variable\n\nThe target variable (also called the dependent variable or label) is what we want the model to predict. All other columns become features (independent variables or predictors) that the model uses to make predictions.\n\nWe use `Overall Rating` as our target because it is the most comprehensive measure of customer satisfaction. The model will learn the relationship between customer demographics, preferences, and behavior (features) and the rating they give (target).\n\n**Regression vs. Classification:**\n- **Regression** predicts a continuous number (e.g., 3.75, 4.2, 1.0). Use when the target is a real-valued number.\n- **Classification** predicts a category (e.g., Low/Medium/High rating). Use when the target is a discrete category.\n\nSince `Overall Rating` contains continuous values, this is a **regression problem**.","metadata":{}},{"cell_type":"code","source":"# Defining Features and Target, Splitting Data\n\n# Define the target variable\nTARGET = 'Overall Rating'\n\n# Important: We drop Food Rating and Service Rating as features\n# to avoid data leakage. These ratings were collected at the\n# same time as Overall Rating and are too directly related.\n# In a real deployment, we would not have these before predicting.\n# Uncomment the lines below if you want to include them as features.\n\n# Drop leakage-prone columns\nfeatures_to_exclude = [TARGET, 'Food Rating', 'Service Rating']\n\nX = df_encoded.drop(columns=[c for c in features_to_exclude if c in df_encoded.columns])\ny = df_encoded[TARGET]\n\nprint(f\"Feature matrix (X) shape: {X.shape}\")\nprint(f\"Target vector (y) shape: {y.shape}\")\nprint(f\"\\nFeature names ({len(X.columns)} total):\")\nfor col in X.columns:\n    print(f\"  - {col}\")\n\nprint(f\"\\nTarget variable statistics:\")\nprint(y.describe().round(3))","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### 8.2 Train-Test Split\n\n**Why do we split the data?**\n\nWe split data into a training set and a test set to simulate what happens when the model encounters new, unseen data. The model learns patterns from the training set. Then we evaluate it on the test set, which it has never seen. If it performs well on the test set, we can be confident it has learned genuine patterns, not just memorized the training data.\n\n**Overfitting vs. Underfitting:**\n- **Overfitting:** The model learns the training data too well, including its noise and random fluctuations. It performs excellently on training data but poorly on test data. Think of a student who memorizes every answer in the textbook but cannot solve any new problem.\n- **Underfitting:** The model is too simple to capture the patterns in the data. It performs poorly on both training and test data. Think of a student who barely studied and cannot answer any question.\n- **The goal** is the sweet spot: a model that generalizes well — performing well on both training and new data.\n\n**Data Leakage:**\nData leakage occurs when information from the test set (or from the target variable) \"leaks\" into the training process. This causes the model to appear much better than it actually is. Classic example: if you use `Food Rating` and `Service Rating` to predict `Overall Rating`, and `Overall Rating` is literally computed as the average of these two, you have not built a predictive model — you have just reverse-engineered the formula.\n\n**What happens if we train on the full dataset?**\nWithout a test set, we have no way to evaluate how well the model generalizes. We might think the model is excellent when it has simply memorized every training sample, which is useless in production.","metadata":{}},{"cell_type":"code","source":"# Train-Test Split (80% train, 20% test)\n\n# test_size=0.2 means 20% of data goes to the test set\n# random_state=42 ensures the split is reproducible\n# Every time you run this code, you get the exact same split\n# This is critical for reproducibility in research and portfolio projects\n\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y,\n    test_size=0.2,\n    random_state=RANDOM_STATE\n)\n\nprint(\"Data split complete.\")\nprint(f\"Training set size: {X_train.shape[0]} samples ({X_train.shape[0]/len(X)*100:.1f}%)\")\nprint(f\"Test set size: {X_test.shape[0]} samples ({X_test.shape[0]/len(X)*100:.1f}%)\")\n\nprint(f\"\\nTraining set target distribution:\")\nprint(y_train.describe().round(3))\nprint(f\"\\nTest set target distribution:\")\nprint(y_test.describe().round(3))","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Feature Scaling\n\n# Feature scaling transforms numerical features to the same scale.\n# StandardScaler converts features to have mean=0 and std=1.\n# This is called standardization (or z-score normalization).\n\n# WHY scale? Consider Age (range 16-70) and Budget (range 1-5).\n# Without scaling, a linear model will weight Age much more heavily\n# than Budget simply because its numerical values are larger,\n# not because it is more important. Scaling removes this bias.\n\n# CRITICAL RULE: Fit the scaler ONLY on training data.\n# Then use it to transform BOTH training and test data.\n# If you fit on the full dataset, test set information leaks into training.\n\nscaler = StandardScaler()\n\n# Fit on training data only, then transform both train and test\nX_train_scaled = scaler.fit_transform(X_train)\nX_test_scaled = scaler.transform(X_test)  # Note: transform only, NOT fit_transform\n\nprint(\"Feature scaling applied.\")\nprint(f\"Mean of first feature after scaling (should be ~0): {X_train_scaled[:, 0].mean():.4f}\")\nprint(f\"Std of first feature after scaling (should be ~1): {X_train_scaled[:, 0].std():.4f}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"---\n\n## 9. Model Building\n\nWe will build and compare three different regression models. Each has different mathematical foundations, assumptions, and behaviors. Comparing multiple models and selecting the best one is standard practice in professional data science.\n\n### Model 1: Linear Regression\n\n**How it works mathematically:**\n\nLinear Regression assumes that the target variable is a linear combination of the features:\n\n`y = β₀ + β₁x₁ + β₂x₂ + ... + βₙxₙ + ε`\n\nWhere:\n- `y` is the predicted rating\n- `β₀` is the intercept (the baseline prediction when all features are 0)\n- `β₁, β₂, ..., βₙ` are coefficients (how much each feature contributes)\n- `x₁, x₂, ..., xₙ` are the feature values\n- `ε` is the error term (the part of y that the linear model cannot explain)\n\nThe model finds the coefficients that minimize the **Residual Sum of Squares (RSS)** — the sum of squared differences between actual and predicted values.\n\n**Assumptions:**\n1. Linearity: The relationship between features and target is linear.\n2. Independence: Observations are independent of each other.\n3. Homoscedasticity: The variance of errors is constant across all levels of the features.\n4. Normality: Errors are normally distributed.\n5. No multicollinearity: Features are not highly correlated with each other.\n\n**Strengths:** Fast to train. Highly interpretable (you can see exactly how much each feature contributes). Works well when the linear assumption holds.\n\n**Weaknesses:** Cannot capture non-linear relationships. Sensitive to outliers. Sensitive to multicollinearity.","metadata":{}},{"cell_type":"code","source":"# Model 1 - Linear Regression\n\n# Initialize the model\nlr_model = LinearRegression()\n\n# Train the model on the scaled training data\n# .fit() computes the optimal coefficients β₀, β₁, ..., βₙ\n# It uses the Ordinary Least Squares (OLS) algorithm internally\nlr_model.fit(X_train_scaled, y_train)\n\n# Generate predictions on the test set\nlr_predictions = lr_model.predict(X_test_scaled)\n\nprint(\"Linear Regression model trained successfully.\")\nprint(f\"\\nModel Coefficients (Feature Importance):\")\ncoef_df = pd.DataFrame({\n    'Feature': X_train.columns,\n    'Coefficient': lr_model.coef_\n}).sort_values('Coefficient', key=abs, ascending=False)\nprint(coef_df.round(4).head(10).to_string(index=False))\nprint(f\"\\nIntercept: {lr_model.intercept_:.4f}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"**Interpreting coefficients:**\n\nA positive coefficient means that as that feature increases by 1 unit (after scaling), the predicted rating increases by the coefficient amount. A negative coefficient means an inverse relationship. Features with larger absolute coefficient values have more influence on the prediction.\n\nFor example, a coefficient of 0.5 for `Budget_encoded` means that customers with higher budgets tend to give higher ratings, all else being equal.","metadata":{}},{"cell_type":"markdown","source":"### Model 2: Decision Tree Regressor\n\n**How it works mathematically:**\n\nA Decision Tree works by recursively partitioning the feature space. At each node, it asks a question: \"Is feature X greater than threshold T?\" Based on the answer, data points are routed to the left or right branch. This continues until leaf nodes are reached, which contain the average rating of all training samples that fell into that region.\n\nThe splitting criterion for regression trees is typically **mean squared error (MSE)**. At each potential split, the algorithm chooses the feature and threshold that minimize the weighted MSE of the two resulting groups.\n\n**Assumptions:** None of the linear regression assumptions apply. Decision trees are non-parametric.\n\n**Strengths:** Can capture non-linear relationships. Requires no feature scaling. Highly interpretable (you can visualize and explain the tree). Handles outliers better than linear regression.\n\n**Weaknesses:** Prone to overfitting when the tree is allowed to grow deep (it will essentially memorize training data by creating one leaf per training sample). High variance — small changes in training data can produce very different trees.","metadata":{}},{"cell_type":"code","source":"# Model 2 - Decision Tree Regressor\n\n# max_depth limits how deep the tree can grow, preventing overfitting.\n# A very deep tree will perfectly memorize training data (overfit).\n# A very shallow tree (max_depth=1) will underfit.\n# max_depth=5 is a reasonable starting point.\n\ndt_model = DecisionTreeRegressor(\n    max_depth=5,          # Maximum tree depth - prevents overfitting\n    min_samples_split=5,  # Minimum samples required to split a node\n    min_samples_leaf=3,   # Minimum samples required at each leaf\n    random_state=RANDOM_STATE\n)\n\n# Note: Decision trees do not require feature scaling\n# We use X_train (unscaled) for the tree models\ndt_model.fit(X_train, y_train)\ndt_predictions = dt_model.predict(X_test)\n\nprint(\"Decision Tree Regressor trained successfully.\")\nprint(f\"Tree depth: {dt_model.get_depth()}\")\nprint(f\"Number of leaves: {dt_model.get_n_leaves()}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Model 3: Random Forest Regressor\n\n**How it works mathematically:**\n\nRandom Forest is an **ensemble method** that builds many decision trees (a \"forest\") and averages their predictions. It combines two key ideas:\n\n1. **Bagging (Bootstrap Aggregating):** Each tree is trained on a different random sample of the training data (drawn with replacement). This introduces diversity — each tree learns slightly different patterns.\n\n2. **Feature Randomness:** At each split in each tree, only a random subset of features is considered. This further reduces correlation between trees and prevents all trees from making the same splits.\n\nThe final prediction is the **average** of all trees' predictions. Because the errors of individual trees tend to cancel each other out when averaged, the ensemble has much lower variance than any individual tree.\n\n**Strengths:** Much more robust to overfitting than a single decision tree. Often achieves state-of-the-art performance without extensive tuning. Handles high-dimensional data well. Provides feature importance scores. Robust to outliers.\n\n**Weaknesses:** Much slower to train and predict than a single tree (must train n_estimators trees). Less interpretable (it is a \"black box\" — hard to explain why a specific prediction was made). Requires more memory.","metadata":{}},{"cell_type":"code","source":"# Model 3 - Random Forest Regressor\n\n# n_estimators: number of trees to build (more trees = more stable,\n# but slower to train). 100 is a standard starting point.\n\nrf_model = RandomForestRegressor(\n    n_estimators=100,       # Build 100 decision trees\n    max_depth=8,            # Maximum depth of each tree\n    min_samples_split=4,    # Minimum samples to consider a split\n    min_samples_leaf=2,     # Minimum samples at each leaf\n    max_features='sqrt',    # At each split, consider sqrt(n_features) features\n    random_state=RANDOM_STATE,\n    n_jobs=-1               # Use all available CPU cores for parallel training\n)\n\nrf_model.fit(X_train, y_train)\nrf_predictions = rf_model.predict(X_test)\n\nprint(\"Random Forest Regressor trained successfully.\")\nprint(f\"Number of trees: {rf_model.n_estimators}\")\nprint(f\"Number of features used: {rf_model.n_features_in_}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null}]}